{% extends 'base.html.twig' %}

{% block body %}


<h1 class="container mt-5 mb-5 d-flex justify-content-center">Add new deliverer</h1>

<div class="container d-flex justify-content-center">
    <div class="form-group w-50 mt-3">
        {{ form(form) }}
    </div>
</div>

{% endblock %}
